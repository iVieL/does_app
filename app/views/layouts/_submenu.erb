<div class="sub-menu" id="sub-menu">
  <% str = object.to_s %>
  <% if !object.nil? and !str.blank? %>
    <nav>
      <ul>
        <% eval(str.gsub('&quot;', '\'')).each do |target| %>
          <li><%= link_to target[0], target[1] %></li>
        <% end %>
      </ul>
    </nav>
  <% end %>
</div>
